apiVersion: v1
kind: ServiceAccount
metadata:
  name: notiflex-api
  namespace: notiflex
  annotations:
    # Workload Identity로 이 KSA를 GCP 서비스 계정에 연결한다.
    # 서비스 계정 키 파일 없이 Secret Manager에 접근한다.
    iam.gke.io/gcp-service-account: notiflex-secrets@project-9d5c279f-44bf-42c9-af2.iam.gserviceaccount.com
