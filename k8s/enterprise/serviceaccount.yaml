apiVersion: v1
kind: ServiceAccount
metadata:
  name: notiflex-api
  namespace: enterprise
  annotations:
    # SMB 테넌트와 같은 GCP 서비스 계정을 쓰되, 네임스페이스별로 따로 바인딩한다.
    iam.gke.io/gcp-service-account: notiflex-secrets@project-9d5c279f-44bf-42c9-af2.iam.gserviceaccount.com
