apiVersion: v1
kind: Service
metadata:
  name: notiflex-api
  namespace: enterprise
  labels:
    app: notiflex-api
    tier: enterprise
spec:
  type: ClusterIP
  selector:
    app: notiflex-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
