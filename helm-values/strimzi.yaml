# Strimzi operator는 worker-pool에 올린다.
# default-pool은 ArgoCD와 관측 스택이 차지하고 있어 여유가 없다.
nodeSelector:
  cloud.google.com/gke-nodepool: worker-pool

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 384Mi
